% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_table_csv.R
\name{extract_table_csv}
\alias{extract_table_csv}
\title{Look for and extract a table anywhere within a CSV file.}
\usage{
extract_table_csv(
  path,
  table_marker = NULL,
  start_column = NULL,
  header_regex = NULL,
  ignore_case = TRUE,
  exact_match = TRUE,
  max_blank_rows = Inf,
  na_values = c("", NA),
  safely = TRUE,
  id_cols = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{path}{Path to CSV file}

\item{table_marker}{Optional string or regex identifying the table marker row}

\item{start_column}{Optional name of a known header column}

\item{header_regex}{Optional regex to validate header row contents}

\item{ignore_case}{Logical; ignore case when matching text}

\item{exact_match}{Logical; exact matching for table_marker. Default TRUE.}

\item{max_blank_rows}{Number of blank rows tolerated between marker and header. Default Inf.}

\item{na_values}{Values treated as NA}

\item{safely}{Logical; return NULL instead of error on failure}

\item{id_cols}{Logical; add file/table metadata columns}

\item{verbose}{Logical; emit detailed progress messages}

\item{...}{Unused (for interface compatibility)}
}
\value{
A tibble, or NULL if safely = TRUE and no table found
}
\description{
Searches a CSV file for a table using one of two strategies:
\itemize{
\item By locating a marker row (e.g., "TABLE: RESULTS"), after which the
next non-empty row is treated as the header.
\item By locating a header row that contains a known column name
(\code{start_column}), analogous to \code{extract_table()} for Excel files.
}

Exactly one of \code{table_marker} or \code{start_column} must be supplied.
}
